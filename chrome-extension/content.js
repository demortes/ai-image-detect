const images = document.querySelector("img");

if(images){
    images.forEach(image => {
        // Check image for fbcdn or other domains, then check 
        // if AI/Deepfake using something like sightengine.
        // Requires api secret, might need to involve an API
        // Keep costs under control, a login? 

    });
}